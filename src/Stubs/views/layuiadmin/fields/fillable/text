
                <div class="layui-form-item">
                    <label class="layui-form-label">{{field.Name}}</label>
                    <div class="layui-input-block">
                        <textarea name="{{field.name}}" id="{{field.name}}-field" lay-verify="required" placeholder="请输入" class="layui-textarea">{{ old('{{field.name}}', ${{model}}->{{field.name}} ) }}</textarea>
                    </div>
                </div>
